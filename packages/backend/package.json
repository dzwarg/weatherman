{
  "name": "@weatherman/backend",
  "version": "0.1.0",
  "description": "Backend post-deployment tests for Weatherman",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:integration": "vitest run --config vitest.config.js tests/post-deployment"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.5",
    "vitest": "^2.1.5"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}
