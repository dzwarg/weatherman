# Production Server Configuration
# Copy this file to .env and fill in your production values

# Server Configuration
PORT=3000
NODE_ENV=production

# Weather API Configuration
# Get your API key from https://openweathermap.org/api
WEATHER_API_KEY=your_production_api_key_here
WEATHER_API_URL=https://api.openweathermap.org/data/2.5

# Claude API Configuration (optional - uses fallback if unavailable)
# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your_production_key_here
CLAUDE_MODEL=claude-3-5-sonnet-20241022

# Frontend Configuration - CORS Allowed Origins
# Single origin:
# FRONTEND_URL=https://weatherman.app
# Multiple origins (comma-separated):
ALLOWED_ORIGINS=https://weatherman.app,https://www.weatherman.app

# Logging Configuration (Optional)
# LOG_LEVEL=info
# LOG_FORMAT=json

# Rate Limiting Configuration (Optional)
# Override default rate limits if needed
# WEATHER_RATE_LIMIT=100
# RECOMMENDATIONS_RATE_LIMIT=500

# Security Headers (Optional)
# TRUST_PROXY=true  # Enable if behind a reverse proxy (nginx, load balancer)

# Static File Serving (Optional - Monolith Deployment)
# Set to 'true' to serve frontend static files from server
# Requires frontend to be built first: npm run build --workspace=@weatherman/frontend
# SERVE_FRONTEND=false  # Default: false (use separate deployments)
